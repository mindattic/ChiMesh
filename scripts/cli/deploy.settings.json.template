{
  "_comment": "Copy this file to deploy.settings.json (no .template suffix) and fill in real FTP credentials. The .gitignore excludes deploy.settings.json so credentials never get committed.",

  "FtpHost":        "ftp.example.com",
  "FtpPort":        21,
  "FtpUsername":    "user@example.com",
  "FtpPassword":    "REPLACE_ME",
  "FtpRemotePath":  "/mindattic.com/chimesh",
  "FtpUseSsl":      true,
  "FtpPassive":     true,

  "_FtpInsecure_comment": "Skip TLS cert validation. Default true (covers shared-hosting FTPS where the cert doesn't match the FTP hostname/IP). Set false when you control the cert chain and want strict validation.",
  "FtpInsecure":    true
}
