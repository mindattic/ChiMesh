{
  "name": "chimesh",
  "version": "1.0.0",
  "private": true,
  "description": "Project metadata for ChiMesh (Chicago LoRa Meshtastic proof-of-mesh build). HTML rendering + deploy now run through the MindAttic.Deploy pipeline (see .claude/commands/deploy.md)."
}
